allow radio ztehw_sysfs:file { read getattr open };
allow radio touchtune_sysfs:file { read getattr open write };

# rfs_access needs this
allow radio system_app_data_file:dir search;
allow radio lge_touch_sysfs:file rw_file_perms;
allow radio unlabeled:dir search;
allow radio unlabeled:dir getattr;
allow radio unlabeled:file { read write getattr };
allow radio unlabeled:file open;
allow radio baseband_prop:property_service set;